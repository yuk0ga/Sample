<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android">
    <data>
        <variable name="mainActivity" type="com.example.koga.sample.MainActivity"/>
        <variable name="areaSetter" type="com.example.koga.sample.AreaSetter"/>
    </data>
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal">
        <TextView
            android:id="@+id/list_item_floor"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="15dp"
            android:textAlignment="center"
            android:foreground="?attr/selectableItemBackground"
            android:text="@{mainActivity.floorList}"
            android:onClick="@{mainActivity.floorList.onButtonClick}"/>

        <TextView
            android:id="@+id/list_item_area"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="15dp"
            android:textAlignment="center"
            android:foreground="?attr/selectableItemBackground"
            android:text="@{areaSetter.area}"
            android:onClick="@{areaSetter.area.onButtonClick}"/>
    </LinearLayout>
</layout>